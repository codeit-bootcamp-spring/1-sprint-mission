spring:
  application:
    name: discodeit

discodeit:
  repository:
    type: file  #jcf | file
    file-directory: .discodeit

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    org.springframework: info

server:
  port: 8080