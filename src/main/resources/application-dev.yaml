server:
  port: 8080   # 개발용 포트

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/discodeit
    username: discodeit_user
    password: discodeit1234

logging:
  level:
    com.sprint.mission.discodeit: debug   # 우리 코드의 로그 레벨: 디버깅용
