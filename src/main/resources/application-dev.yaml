server:
  port: 8081
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeitdb
    username: root
    password: Abcd123@
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    #        default_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: info  #원래 debug
    org.hibernate.orm.jdbc.bind: trace
    root: info #원래 debug