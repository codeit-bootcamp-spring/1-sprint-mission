server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

discodeit :
  repository :
    type: jcf #jdf | file
    file-directory: .discodeit