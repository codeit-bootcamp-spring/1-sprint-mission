spring:
  application:
    name: discodeit
  servlet:
    multipart:
      maxFileSize: 10MB # 파일 하나의 최대 크기
      maxRequestSize: 30MB
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: sg0114
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui
    enabled: true

discodeit:
  repository:
    type: file
    file-directory: .discodeit