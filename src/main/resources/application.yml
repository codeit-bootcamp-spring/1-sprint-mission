server:
  port: 8080

spring:
  application:
    name: discodeit
  servlet:
    multipart:
      maxFilesize: 10MB # 파일 하나의 최대 크기
      maxRequestSize: 30MB #  한 번에 최대 업로드 가능 용량

discodeit:
  repository:
    type: file  # 파일 저장소 설정 유지
    file-directory: .discodeit