server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/discodeitdb_prod
    username: root
    password: Abcd123@
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    #        default_batch_fetch_size: 100

logging:
  level:
    org.hibernate.SQL: info
    org.hibernate.orm.jdbc.bind: trace
    root: info